# Store backend: tinydb (default) or mongo
STORE_BACKEND=tinydb

# Mongo settings (only used when STORE_BACKEND=mongo)
MONGO_URL=mongodb://localhost:27017
MONGO_DB=rag_lab
MONGO_COLLECTION=store_documents

# TinyDB file (only used when STORE_BACKEND=tinydb)
TINYDB_FILE=tinydb.json
